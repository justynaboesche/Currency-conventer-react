import "./style.css";

const Footer = ({ title }) => (
    <footer className="footer">
        <p className="footer__paragraph">
            { title }
        </p>
    </footer>
);


export default Footer;